<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Exam Results</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f4f7f6; display: flex; justify-content: center; padding: 40px; }
        .results-container { background: white; width: 100%; max-width: 800px; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
        .score-banner { background: #2563eb; color: white; padding: 30px; border-radius: 8px; text-align: center; margin-bottom: 30px; }
        .score-num { font-size: 3rem; font-weight: bold; }
        .q-review { border-bottom: 1px solid #eee; padding: 20px 0; }
        .status-badge { display: inline-block; padding: 4px 12px; border-radius: 20px; font-size: 0.8rem; font-weight: bold; text-transform: uppercase; }
        .correct { background: #dcfce7; color: #166534; }
        .incorrect { background: #fee2e2; color: #991b1b; }
        .btn { display: inline-block; margin-top: 20px; padding: 12px 24px; background: #2563eb; color: white; text-decoration: none; border-radius: 6px; }
    </style>
</head>
<body>
    <div class="results-container">
        <div class="score-banner">
            <h1>Test Results</h1>
            <div class="score-num"><%= score %> / <%= total %></div>
            <p><%= Math.round((score/total)*100) %>% Proficiency</p>
        </div>

        <h3>Question Review</h3>
        <% results.forEach(r => { %>
            <div class="q-review">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <strong>Question <%= r.questionNumber %></strong>
                    <span class="status-badge <%= r.isCorrect ? 'correct' : 'incorrect' %>">
                        <%= r.isCorrect ? 'Correct' : 'Incorrect' %>
                    </span>
                </div>
                <p style="margin: 10px 0; color: #4b5563;"><%= r.questionText %></p>
                <div style="font-size: 0.9rem;">
                    <div style="color: #6b7280;">Your Answer: <span style="font-weight: bold; color: #1f2937;"><%= r.userAns || '(Empty)' %></span></div>
                    <% if(!r.isCorrect) { %>
                        <div style="color: #059669; margin-top: 5px;">Correct Answer: <strong><%= r.correctAns %></strong></div>
                    <% } %>
                </div>
            </div>
        <% }) %>

        <div style="text-align: center; margin-top: 30px;">
            <a href="/profile" class="btn">View My Progress</a>
            <a href="/exam-launcher" class="btn" style="background: #64748b;">Try Another Test</a>
        </div>
    </div>
</body>
</html>